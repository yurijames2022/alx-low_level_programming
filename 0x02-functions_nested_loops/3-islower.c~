#include "main.h"

/**
 * _islower - Entry point
 *
 * Description: check for lowercase character
 * Return: 1 if lowercase, 0 if otherwise
 */
int _islower(int c)
{
	int i;

	for (i = 'a'; i <= 'z'; i++)
	{
		_putchar(i);

		if (c == i)
		{
			return (1);
		}


	}
	return (0);
}
