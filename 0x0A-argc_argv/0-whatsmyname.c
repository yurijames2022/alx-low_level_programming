#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 *@argc: The argument count
 *@argv: The argument values in an array of strings
 * Return: Returns 0 to show success
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
