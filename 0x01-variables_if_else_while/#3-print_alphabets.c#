#include <stdio.h>
#include
/**
 * main - Entry point
 * Description: code for displaying alphabet in lowercase and uppercase
 * Return: 0 (success)
 */
int main(void)
{
	int i, j;
	j = toupper(i);
	for (i = 'a'; i <= 'z'; i++)
	{
		putchar(i);
		putchar(j);
	}
	putchar('\n');
	return (0);
}
