#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Print numbers with putchar
 *
 * Return: 0
 */
int main(void)
{
	int i;

	for (i = 1; i < 10; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
